apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sample-app
spec:
  parentRefs:
    - name: public
      namespace: platform-ingress
  rules:
    - backendRefs:
        - name: sample-gateway
          port: 80
